# Canonical Promotion Receipt — R4.1

Date: 2026-09-03
Disposition: `PROMOTED_TO_CANONICAL_IN_HOUSE_SOP`
Scope: project-agnostic / thread-agnostic process and cold-start authority.

## Basis
- operator explicitly corrected the governing intent: for nontrivial work, PDVER + hostile engineering + Semantic Helix + Attention Reservoir + Loop+ + OARR + CSC, amplified by additive AI co-processor strengths, are the base-tier functional approach rather than merely optional named campaign machinery;
- the correction preserves proportionality: standing functions are mandatory for nontrivial work, while visible choreography and ceremony are not;
- PDVER ordering is resolved as `PROBE -> DERIVE -> VERIFY -> EMBODY -> RECURSE`, while post-embodiment readback/attack remains required where consequence matters;
- the R4.0 fixed 20-pass universal research default is removed in favor of a justified bounded campaign;
- exact R4.0 parent ZIP and R3.1 ancestry are preserved without changing historical identity;
- active universal surfaces remain project/person/runtime-state clean;
- primary verifier PASS;
- hostile mutation suite **17/17 rejected**, with semantic mutations re-manifested before verification so rejection exercises semantic/authority guards rather than only byte drift;
- final release requires a post-mutation linear inspection of every outer package file before the archive is handed off; the verifier now independently enforces canonical manifest identity plus required canonical document/fixed-member presence so a re-manifested omission cannot pass as a valid package.

## Authority effect
R4.1 supersedes R4.0 as the canonical in-house process/cold-start SOP default. It does not create product, domain, architecture, legal, or scientific-result authority. Project-local standing obligations continue to govern their own scopes.

`CANONICAL_PROCESS_DEFAULT != UNIVERSAL_DOMAIN_TRUTH`
`BASE_TIER_FUNCTIONAL_OBLIGATIONS != MANDATORY_LINEAR_PIPELINE`
