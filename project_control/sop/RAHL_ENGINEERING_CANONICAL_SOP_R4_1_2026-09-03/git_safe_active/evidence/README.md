# Evidence Boundary

The receiver continuity qualification artifacts in this directory derive from exact source evidence for the H01–H10 continuity mechanism family. They are **evidence**, not active universal source code and not a claim that the isolated candidate was promoted into any live receiver.

## Git-safe derivative boundary
In the canonical R4.1 carrier, historical/project-specific paths inside these evidence files are preserved because changing them would destroy evidence identity. **This Git control copy is explicitly a transformed public-safe derivative:** machine-local path literals and one test-fixture key literal are redacted where declared in the parent `GIT_SAFE_TRANSFORMS.json`. Exact canonical source hashes remain preserved in `SOURCE_ACTIVE_FILE_HASHES.json`.

The embedded `MANIFEST_SHA256.json` and `VERIFY_CANONICAL_SOP.py` are retained as canonical-source reference content; they describe/verify the original R4.1 carrier, not these transformed derivative bytes. Do not use the Git-safe derivative as a substitute for canonical carrier verification.

`SOURCE_IDENTITY != GIT_SAFE_REPRESENTATION`
