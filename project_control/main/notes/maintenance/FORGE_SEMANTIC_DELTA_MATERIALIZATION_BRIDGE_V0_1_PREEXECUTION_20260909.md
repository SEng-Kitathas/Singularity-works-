# Forge Semantic Delta -> Materialization Bridge v0.1 — Pre-Execution

Date: 2026-09-09 UTC
Status: **FROZEN CORE CANDIDATE / NOT EXECUTED**
Binding currentness: project-control `4783bf040546d0ba331879a104006540030d51fc` / CHECKPOINT `a537abe0c6b98ef5393b5fd0a8ea14cd5c7e270da1f3d8f46155419f189b0a26`, fresh-clone verifier PASS 253.

This candidate does not duplicate the already-qualified source materializer. It bridges the newly qualified semantic replay plan to exact materialization anchors. Semantic intent does not uniquely determine source syntax, so the bridge resolves removed semantic keys to exact unique base fact/evidence/source anchors with authority NONE; an explicit lowering policy still supplies replacement bytes.

The first discriminator uses the same qualified PyGoat homepage-route removal represented by Semantic Delta Replay v0.1.3. It must prove that the replay plan, bridge anchor, incumbent SourcePatch, isolated materialization, re-lowered target bundle, and reverse rollback all agree exactly while the real PyGoat repository remains untouched.

`SEMANTIC_INTENT != UNIQUE_SOURCE_SYNTAX`
`DELTA_TO_PATCH_BRIDGE != SOURCE_LOWERER`
`BRIDGE_AUTHORITY = NONE`
