# Recovery / Zombie Architecture
Owns immutable attempt capture, append-only operational journal, transactional writes, recovery bundles, crash-only startup posture and deliberate kill/recovery qualification. Ergo consumes this state; it does not invent it.
