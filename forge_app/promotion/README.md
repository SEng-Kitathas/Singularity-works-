# Promotion
Only explicitly qualified mechanisms enter this surface. Promotion requires named evidence, exact lineage, bounded claims, readback and a demotion trigger. Nothing is promoted because it looks complete.
